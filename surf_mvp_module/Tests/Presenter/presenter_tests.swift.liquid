//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import XCTest
@testable import {{ module_info.project_name  }}

final class {{ module_info.name }}PresenterTest: XCTestCase {

    private var presenter: {{ module_info.name }}Presenter = {{ module_info.name }}Presenter()
    private var view: MockViewController = MockViewController()

    override func setUp() {
        super.setUp()
        presenter = {{ module_info.name }}Presenter()
        presenter.router = MockRouter()
        view = MockViewController()
        presenter.view = view
    }

    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }

    func testShowScreen() {
        presenter.router.showScreen(withParameter: <#Parameter#>)
        XCTAssertTrue((presenter.router as! MockRouter).showScreenWasCalled)
    }

    func testSetupInitialState() {
        presenter.view.setupInitialState()
        XCTAssertTrue((presenter.view as! MockViewController).setupInitialStateWasCalled)
    }

    final class MockRouter: {{ module_info.name }}RouterInput {
        var showScreenWasCalled: Bool = false

        /// Method for transition to other module with custom parameter
        func showScreen(withParameter parameter: <#Parameter#>) {
            showScreenWasCalled = true
        }
    }

    final class MockViewController: {{ module_info.name }}ViewInput {
        var setupInitialStateWasCalled: Bool = false

        func setupInitialState() {
            setupInitialStateWasCalled = true
        }
    }

}
